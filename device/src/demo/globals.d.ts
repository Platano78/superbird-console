/** Injected by vite.config.ts `define` from the SUPERBIRD_DEMO env var at
 *  build time. Always defined (literal `true`/`false`), so a production build
 *  constant-folds every demo branch guarded by it -- zero runtime cost when
 *  the flag is off. */
declare const __SUPERBIRD_DEMO__: boolean
